name: Backporter
on: [issue_comment]

jobs:
  issue_commented:
    # This job only runs for issue comments
    name: Issue comment
    if: contains(${{ github.event.issue.comment }}, '/backport')
    runs-on: ubuntu-latest
    steps:
    - name: echo-out
      run: echo The event is "$EVENT"
      env: 
        EVENT: ${{ github.event }}
      
