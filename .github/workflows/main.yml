name: Backporter
on: [issue_comment, workflow_dispatch]

jobs:
  issue_commented:
    if: github.event.comment.body == '/backport'
    name: Issue comment
    runs-on: ubuntu-latest
    steps:
    - name: echo-out
      run: echo '${{ toJSON(github.event) }}'
